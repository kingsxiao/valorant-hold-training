# 架枪训练 · Hold Angle Trainer（WebGL）

一个用 **Three.js (WebGL2)** 实现的第一人称架枪/对枪训练器。移动、武器数值按 Valorant 公开资料调校，目标是把"急停—开枪—压枪—反应"这套肌肉记忆在浏览器里练起来；可选**闪光干扰**（KAY/O / 斯凯 / 火男 / Yoru / Breach / Reyna / Gekko 七种敌方闪光·致盲道具按维基数值 1:1 还原），练"听声背身躲闪再回防"。

> **关于素材的说明**：Riot 的游戏资产（模型/贴图/音效）受版权保护，本项目**不提取、不打包、不复刻**官方资源。
>
> **当前内置的开源模型**（`public/models/`，均已 gltf-transform 量化压缩 + 贴图 JPEG 重编码，且无需解码器）：
> - `agent.glb` — ["X Bot"](https://github.com/mrdoob/three.js/tree/master/examples/models/gltf)（Mixamo，经 three.js 官方示例分发），训练靶按实际移速**加权混合 idle/walk/run 动画**，脚步频率与位移同步（加载时自动归一化到 1.8m）；备选 BrainStem 模型在仓库 `models-optional/`（不随部署分发），复制进 `public/models/` 改名即可换用
> - `viewmodel-vandal.glb` — ["AK-47 Kalashnikov" by Mateusz Woliński](https://sketchfab.com/3d-models/ak-47-kalashnikov-2da6b0332a5c4870a854ebea34eddfc3)（**CC-BY 4.0**，经 Objaverse 分发）：真实 PBR 木纹/金属贴图的经典 AKM，作 Vandal 第一人称枪模
> - `viewmodel-phantom.glb` — ["AK 47 Tactical Upgrade" by Mateusz Woliński](https://sketchfab.com/3d-models/ak-47-tactical-upgrade-b15d69e8a5a948819c8c388f97930b9c)（**CC-BY 4.0**，经 Objaverse 分发）：战术导轨 + 消音器造型（自带 bolt carrier / magazine 独立网格，枪机后坐用原生机件），作 Phantom 第一人称枪模。双枪各有按枪面射线实测收敛的逐指握姿，切枪自动重摆
> - `glove.glb` / `hands.glb` — ["Gloved Hand"] / ["Rigged FPS Arms" by J-Toastie](https://poly.pizza/m/XdHWM8uSAO)（Poly Pizza 分发，均 **CC-BY 3.0**；五指手套双手为第一人称主路径，hands 整臂做 IK 袖臂与后备；缺失时逐级回退程序化手臂）
> - 旧版单枪模 `viewmodel.glb`（Quaternius AK47，CC0）备份在 `models-optional/`，改名放回即回退；Sheriff/手枪/刀使用内置程序化模型（带枪机/套筒/转轮活动机件、解剖学分段手部动画）
>
> **替换成你自己的资产**（本地使用你拥有合法权利的文件）：同名覆盖 `public/models/*.glb` 后跑 `npm run optimize:models` 一键压缩；音效放 `public/sfx/`（文件名见目录内说明）。本项目与 Riot Games 无关。

## 运行

```bash
npm install
npm run dev      # http://127.0.0.1:5173
npm run build    # 产物在 dist/
npm run preview
npm run verify   # lint + 单元测试 + 构建（CI 同款门禁）
npm run optimize:models  # 压缩 public/models/*.glb（换入自有模型后执行）
```

要求：桌面版 Chrome/Edge/Firefox（需要 WebGL2 + Pointer Lock），Node ≥ 18。

指针锁定优先请求 `unadjustedMovement`（绕过系统鼠标加速度/平滑，瞄准训练器的关键一致性），旧浏览器自动回退普通锁定。

## 部署

纯静态 SPA，`dist/` 可托管在任意静态服务器 / GitHub Pages / Vercel / Netlify / Cloudflare Pages / Docker。资源全部相对路径（`base: './'`），支持任意子路径部署；字体自托管，无第三方请求。全量约 4.9 MB（GLB 模型已预压缩约占 4.1 MB；JS/CSS/字体 gzip 后合计 ~0.5 MB）。缓存策略、nginx 配置样例与各平台步骤见 **[DEPLOY.md](DEPLOY.md)**。

## 操作

| 按键 | 功能 |
| --- | --- |
| `W A S D` | 移动（步枪 5.4 m/s · 副武器 5.73 · 持刀 6.75） |
| `Shift` | 静步（62.8% 速度 ≈3.39 m/s，无声） |
| `Ctrl / C` | 蹲下（≈1.8 m/s，散布小幅优化） |
| `Space` | 跳跃 |
| `左键` | 开火（全自动武器按住） |
| `右键` | Classic 三连发 |
| `1 / 2 / 3` | 主武器 / 副武器 / 刀（持刀 6.75 m/s） |
| `ESC` | 暂停并呼出菜单 |

鼠标灵敏度与游戏同换算：**0.07°/count × 灵敏度**（即 CS 灵敏度 × 3.18 的关系），菜单里直接填你游戏内的灵敏度即可获得相同的 360° 距离。

**其它行为**：
- `ESC` 暂停时使用游戏时钟——回合计时与 Bot 行为全部冻结，不会因暂停丢回合时间
- 弹药无限（HUD 显示 ∞）——架枪训练不中断节奏，专注急停/预瞄/压枪本身
- 菜单「画质」区可调：分辨率缩放（50%~200%）、自适应分辨率（掉帧自动降最低 60%、恢复后回升）、阴影开关、FPS 面板显隐，设置持久化到 localStorage（隐私模式下静默降级为仅本次会话生效）
- 全套空间音频：敌方枪声/Bot 脚步/机体落地/头盔落地/闪光道具均走 HRTF 声像 + 距离衰减 + 远距低通闷化（听者方位换算与相机矩阵严格一致，可听声辨位）；Bot 脚步与步态落脚帧同拍、音量随移速连续分层（加速渐强），玩家自己的脚步随实际速度轻重变化
- WebGL 上下文丢失自动暂停并提示，恢复后重建材质继续

## 训练模式

只有一种纯粹的架枪对枪训练：开局 3 秒倒计时（GO 后才开始计时），随机延迟后 Bot 以两种方式出掩体——**侧面跑过**（顺跑动方向贯穿缺口，侧身入镜，移动中带概率急停、转回面向你"停步挑战"）与**横向拉出**（肩 peek 面向你横移拉出，到窗口内急停站定对枪，之后缩回墙后；部分拉出 Bot"露头即缩"jiggle-peek，拉到中段立即折返，逼你守住准星等第二拉）。在"击杀时限"内没打中，Bot 开火反击后跑向对面掩体撤离——躲进墙后才出下一波（判负只记统计、不弹提示），你继续架枪——**没有死亡/掉血设定**，训练节奏永不中断，练先开枪能力。墙上只开一个缺口，出生点就在缺口正前方的架枪位，进局即对口架枪；中场木箱可换点位架枪，地面距离标线帮助测距。Bot 持步枪 5.4 m/s 横移，启停走与玩家同一套地面物理（`core/GroundMotion.js`）：拉出加速 ≈0.29s 达全速、counter-strafe 急停 ≈0.147s 停稳（Riot 官方实测 0.160s）—— Bot 出掩体的加速滑行与急停节奏和真人一致。

**训练开关**（菜单"参数"区）：
- 渐进难度：每次击杀后 Bot 出现延迟 ×0.93、横移速度 ×1.02（有保底/封顶），一局之内越打越快
- 缺口位置：缺口在左还是在右由设置决定（左侧 / 右侧二选一），切换即重建地图并持久化——同一个口，练两侧架枪习惯
- Bot 出场侧：可设置固定从左侧 / 右侧掩体后出（默认固定左侧，练同向预瞄），或保留左右随机（读局训练）；两种出掩体风格（跑过/拉出）都遵守该设置，实时生效并持久化

**反馈与激励**：
- 击杀得分：击杀 100 + 爆头 50 + 连杀 ×25；个人最佳持久化（HUD 实时对比，破纪录时结算面板绿色高亮"新纪录！"）
- **预瞄误差采样**：Bot 露头瞬间记录准星与目标的角偏差（度），HUD 实时显示均值 —— 出现后才甩过去的是反应，出现前就贴住的是预瞄
- 结算面板：本局评级（S/A/B/C/D，按得分/分钟）、最长连杀、反应时间分布直方图（红线标均值）、近 10 局得分趋势、对比上局增减角标（反应类自动反转配色），并按本局短板生成一条可执行的训练建议
- 连杀击杀确认音每级升半音（上限 +4），听觉反馈连杀节奏
- **准星与游戏设置 1:1**：设置面板复刻游戏准心页——颜色（8 预设 + 自定义取色器）、轮廓（开关/不透明度/粗细 0-6）、中心点（开关/不透明度/大小 1-6，方块非圆点）、内线/外线（各自的显示/不透明度/水平长度/垂直长度（可解耦联动）/粗细 0-10/间距 0-20/移动误差与开火误差开关 + 倍率 0-3×）、高级三项（开火时淡出（游戏默认开）/移动时淡出/开火误差叠加在准星间距上）；渲染几何同游戏：间距 = 中心到线段内缘、逐元素方形轮廓、带开火误差的线组静止时 +4px 底距。**支持导入/导出游戏准星代码**（`0;P;c;5;…` 同格式——游戏里复制的代码原样可粘，本训练器调好的也能复制回游戏）；旧版简化准星设置自动迁移
- 准星动态误差分移动/开火两路独立信号（内外线按各自开关+倍率扩张，默认形态即游戏原味：跑动外线张开、扫射内线张开）——扩张瞬时可见、收束缓动归位，收束时才是出手时机
- 对枪失败：Bot 原地开火还击（枪口焰/曳光 + 空间枪声 + 子弹掠过声）后缩回，Toast 提示"对枪失败"——纯失败提示，玩家无任何受伤表现，不掉血
- 切枪机械上膛音、倒计时 tick/GO 提示音、回合结束音；回合最后 10 秒计时红色告急
- ESC 暂停面板顶部显示"本局进行中"战绩条

HUD 实时显示：得分/最佳 / 击杀 / 对枪败 / 命中率 / 爆头率 / 反应时间（均值、最快与波动 ±标准差）/ 预瞄误差 / FPS 与 1% low / 当前移速。

## 数值对齐（`src/core/Config.js` 集中管理）

**移动**（公开资料值）：
- 分武器跑速（Fandom 维基各武器页 infobox）：步枪/狙击 5.4 m/s、副武器与 SMG 5.73、霰弹枪 5.06、持刀 6.75；静步 = 62.8%（≈3.39 m/s，Riot 开发者实测走路级精度门槛 61% 速度互证）；蹲 ≈ 34%
- 启停 = 摩擦模型：加速 18.75 m/s²（持步枪实测，按目标速度等比缩放，各档位 ≈0.29s 达标）；减速 = 28.6 m/s² 恒定摩擦 + 3.3/s 速度阻尼 —— 全跑 5.4 m/s 急停：61% 速度 0.055s / 25% 0.104s / 停稳 ≈0.147s，复现 Riot_Classick 官方实测里程碑（0.055/0.104/0.160s）；**反向键无额外减速加成**（摩擦恒大于加速，counter-strafe 不是 CS 机制，官方实测仅省 ~10ms）
- 重力 22 m/s²、跳跃初速 5.9（顶点 ≈0.77m）
- 水平 FOV 103°（垂直随窗口比例换算，与游戏锁水平视场的做法一致）

**武器**（射速/伤害为公开资料值）：

| 武器 | 射速 | 弹匣 | 头/身/腿 | 备注 |
| --- | --- | --- | --- | --- |
| Vandal | 9.75/s | 25 | 160/40/34 | 全距离不衰减 |
| Phantom | 11/s | 30 | 156/39/33 | 20m 起衰减 ×0.897（v9.10） |
| Sheriff | 4/s | 6 | 159/55/46 | 高散布惩罚；30m 起衰减 ×0.909 |
| Ghost | 6.75/s | 15 | 105/30/25 | 30m 起衰减（87/25/21） |
| Classic | 6.75/s | 12 | 78/26/22 | 右键三连发；30m 起衰减（66/22/18） |

**手感机制**：
- 散布数值与公开资料对齐（Fandom 维基数据挖掘表，2026-09 版）：Vandal 首发 0.25°（蹲 0.21°）、走路 3.25°、全速 6.25°、跳跃 10.25°（五把枪各自对表）；蹲走另有 +0.8° 加算惩罚（蹲姿限速支线独立插值），持续连射散布封顶 1°（Vandal）
- 后坐力：程序化生成的弹道表（前 9 发垂直爬升 → 高位平台 → 水平摆动；垂直分量全程不下坠——压枪量到高点封顶，只左右修），水平段结构对齐公开补丁机制：前 6 发（Vandal）/ 8 发（Phantom）水平保护无横偏，其后按 0.6s 换向节拍**先向右漂再拉左**（bo3.gg/VALTRAIN 记载的摆动方向）渐强；跑动时垂直后坐 ×1.8（v6.11 公开改动，按移速比例介入）；停火 0.4s 立即重置（鼓励点射/急停）
- 视角上踢仅为视觉、弹道由表驱动；上踢恢复分武器曲线：步枪 ~10-11/s 快回保持准星可读，Sheriff 5.5/s 重枪慢沉
- 消音武器（Phantom/Ghost）开火视觉同步收敛：枪口焰更小更暗、曳光更淡更细、枪口烟更轻——与更轻的枪声/持枪冲量一致（音、枪感、画三线对齐）；命中落点音也压一档（打墙不比开枪响），Sheriff 大口径落点则加量（.44 打墙更狠）
- 曳光为 240m/s 飞行光段：着色器渐变光带（头白热、尾渐暗渐细的锥形拖尾）+ 段头双层光珠（亮芯+halo）+ 每发亮度/色相微抖 + 末段冲刺拉伸白热；玩家曳光暖黄、Bot 还击红调（对枪瞬间分清弹道归属）；近距（<8m）切整条快闪
- 枪声打感分层：连射热量调制音色（越打越亮越紧，过热后枪管余振）、Sheriff 击锤待击双响（110/185ms 与击锤动画同拍）+ 转轮欠阻尼回弹（一次 ~10° 过冲）、切枪上膛响随各枪 equipTime 校相、爆头"叮"与击杀确认音共用连杀半音升调阶梯
- 击杀/受击反馈物理语言三档：受击（火星沿弹道反弹半球崩射+踉跄幅度随伤害联动）→ 普通击杀（+白闪爆芯弱档+琥珀击杀辉光）→ 爆头击杀（+白闪爆芯强档+头盔顺弹道掀飞与落地火花/哐声+红→金连杀辉光递进+机体拍地闷响）；弹壳落地"叮"、头盔落地"哐"均空间化
- 命中硬表面按材质分色：墙面脆"叩"+碎屑火花，地面闷"噗"+尘雾翻倍；Bot 子弹穿身后打在墙上（弹孔/碎屑/落点音按 240m/s 飞行时间延迟——"枪声→掠过啸→身后嗒"完整时序）
- 音量滑杆内部为感知补偿曲线（幂律 0.62）：小音量下脚步/落点等弱信号不被压进听阈以下，满音量端点不变；爆闪背身躲过时高频压暗（"这颗没闪到你"听得出来），白屏渐褪尾段带道具类型色残像
- 持枪动画：枪机/套筒击发后坐回位、Sheriff 转轮分度+击锤、切枪从下方托起、静止呼吸微摆、挥刀弧线（弹药无限，无换弹动画）
- 第一人称手部（glove.glb 五指手套主路径）：每枪独立腕锚/逐指卷曲握姿（含拇指对握、食指扣扳机位），蒙皮顶点级射线检测保证任意状态（静态/待机微动/开火收紧）零穿模；待机肌腱微动 ±0.8° 防静帧僵死；开火时枪口焰点光同步点亮第一人称渲染通道（枪身+手套瞬时高光）；深灰战术手套材质与护腕环同色系
- 逻辑帧固定 **128Hz**（与游戏服务器 tick 一致），渲染插值，鼠标视角渲染帧直通（零延迟）

**闪光干扰**（可选敌方道具，数值 = Fandom 维基 2026-09 各技能页 + Deployment types 投掷物等级表）：

| 道具 | 弹道 | 起爆 | 最大致盲 |
| --- | --- | --- | --- |
| KAY/O FLASH/drive | Class 2 手雷 18 m/s、重力系数 0.3（=2.94 m/s²），抛掷翻滚、落地/撞墙弹跳（恢复系数 0.32） | 总引信 1.6s；首次弹跳改 0.8s 引信（v10.06，不延长剩余）+ 爬升嗡鸣 | 2.25s（v11.08） |
| Skye Guiding Light | 追踪鹰导弹 18 m/s 无重力，先穿缺口再追踪玩家视线前方（急转自然减速→近距盘旋），绿光尾迹、充能满转橙光+提示音 | 贴近且充能满（飞行 0.75s）→ 0.3s 起爆预备；飞满 2s 寿命自动激活（v8.01） | 1s→2.25s 随飞行充能（v5.07/v10.00） |
| Phoenix Curveball | Fixed 曲线导弹（弧长参数化贝塞尔、恒速），左/右曲绕墙角划弧，火焰拖尾 | cast→起爆 0.6s（v11.00）；撞墙即熄灭（无致盲） | 1.5s（v5.01） |
| Yoru Blindside | Class 3 碎片 29 m/s、重力系数 0.45（=4.41 m/s²），低伸弹道撞面 | **飞行不可见也无声**（v11.10）——撞面才显形 + 0.6s 预备（v2.06）；2s 未撞面消散 | 1.5s（v11.08，标准衰减曲线） |
| Breach Flashpoint | Placement 穿墙放置：charge 直接出现在玩家侧墙前面（部署距 35m/穿墙深 10m） | 到位后 0.5s 预备（v1.07），红橙脉冲呼吸灯为唯一转身提示；起爆为**贴墙竖直光柱**爆闪（本体形态，非球形扩散） | 2.25s（v11.08） |
| Reyna Leer | **近视系（非白闪）**：导弹直线穿地形到 10m 部署距悬停，紫晶眼球盯人 | 0.4s 睁眼（v5.07）后施加近视 1.6s——看清"瞳孔"即持续命中（6m 视界）；**100HP 可击落**；近视附带 **Deafened**（维基 Status Effect：Nearsight 者同时被聋——音频闷化，与视野收束同步） | 近视 1.6s（转开即褪） |
| Gekko Dizzy | **官方模型**；Class 2 投掷（同 KAY/O 物理），0.65s 激活后减速半空悬停 | 活跃 1s（v9.08）内对 45m 视线目标 0.35s 锁定（v7.12）喷等离子；**20HP 可击落** | 等离子全屏 2s = 1s 满效+1s 渐褪（**转身不可避**） |

- 致盲判定对齐维基定性机制：需要视线（LOS）+ 朝向角 + 距离三因子——直视起爆点满时长、背对仅"轻微短暂"（×0.08）、超远距离免疫；致盲到期后白屏 **1 秒渐褪**（该 1s 为维基确认值），**整屏纯白**（本体口径：无渐变边缘、无残像色），白屏之下准星/弹药/计分仍可见（与游戏一致）
- 起爆后敌方随即拉出（0.55-1.0s 后下一波 peek 提前，"闪拉"配合）；投掷起点在墙后（模拟看不见的敌人），KAY/O 二选一弹道：越墙顶高位爆 / 穿缺口低位爆
- 音效全部 WebAudio 合成并按道具特征还原：KAY/O 出手风声+弹跳硬响+0.8s 爬升嗡鸣（飞行途中无声——v3.06 已移除）；斯凯鹰 7.5Hz 振翅循环（移动声源、听声辨位）+充能提示；火男火球灼烧渐强（v1.06：音频提示何时该背身）；Yoru 显形后裂隙升鸣（飞行全程无声=v11.10）；Breach 放置闷雷+双联冲击；Reyna 魂雾升腾+悬停低鸣；Dizzy 出手啾叫+等离子糊屏"啪叽"；起爆"爆闪"分层炸响各带特征尾音
- 模型：斯凯鹰与 Dizzy 用**官方提取模型**（`public/models/ability-{hawk,dizzy}.glb`，Rocklan 模型包 .blend→GLB 转换+MRS 补丁，翼骨/尾骨运行时扇动）；其余为程序化原创近似（枪金属罐雷+琥珀警示灯 / 炽焰核心+光晕 / 维度碎片 / 贴墙 charge / 紫晶眼球）
- 白闪起爆判定同上三因子；近视（Leer）/等离子（Dizzy）走独立屏效（紫雾虚焦+**Deafened 闷音** / 绿紫史莱姆糊屏——本体 goop 为绿紫外星黏液），命中判定与白闪不同：Leer 看清瞳孔才命中、Dizzy 只判 LOS（转身不可避）

## 架构

```
src/
  core/     Engine(渲染/双pass/固定步长循环/FPS统计/上下文恢复) · Config(全部数值) · Input(原始鼠标输入)
            Audio(WebAudio合成音效, HRTF听声辨位) · stats(统计口径) · Rng(可复现PRNG)
  player/   Player(移动模型/碰撞/视角)
  weapons/  WeaponSystem(状态机:开火/切枪/持枪动画) · ViewmodelFactory(程序化枪模+手臂建模)
            HandsRig(GLB手部IK式装配) · ballistics(伤害衰减/散布, 纯函数)
  world/    World(AABB碰撞+射线+命中球检测) · MapBuilder(程序化地图, 几何合并)
            FlashSystem(闪光干扰:七类投掷物弹道/引信/起爆/致盲/近视/等离子+官方模型挂载+可击毁) · flashMath(致盲模型, 纯函数)
            FX(曳光/弹孔/枪口焰对象池) · Textures(程序化PBR纹理) · ModelTexturing(GLB贴图)
  entities/ Bot(命中球体/peek状态机/骨骼动画混合) · BotManager(架枪对枪轮次/统计)
  ui/       Crosshair(canvas准星:游戏同款几何/动态误差) · crosshairCode(准星设置模型+游戏代码parse/export, 纯逻辑)
            HUD(节流更新+FPS图) · Menu(设置持久化+准星编辑器) · ResultPanel(回合结算)

tests/      纯逻辑单元测试（Vitest）：碰撞/射线/弹道/伤害/散布/统计/评级建议/反应去重/PRNG/HRTF方位换算/准星代码解析往返/闪光致盲模型
```

质量保障：
- `npm run verify` = ESLint + Vitest（147 用例）+ 生产构建；GitHub Actions CI 对每次 push/PR 运行同款门禁
- 纯逻辑（弹道、散布、统计、碰撞、方位换算）全部抽成无渲染依赖的函数并覆盖测试
- 关键回归用例：HRTF 听者方位换算与 three.js 相机矩阵交叉验证（修复过一次前后颠倒）

性能设计（实测 100~145 FPS @2K，dev 模式）：
- 静态地图按材质合并，全场个位数 draw call；命中/视线为解析射线（不遍历网格）
- 曳光/弹孔/伤害数字全对象池，主循环零内存分配（复用向量）
- HUD 只在文本变化时写 DOM；碰撞/实体数据为扁平数组
- 程序化纹理的 Sobel 法线转换为行索引外提的热循环实现；首屏载入页覆盖初始化期（生产构建 ~350ms 就绪）
- 真实阴影（太阳投影，静态几何已合并，实测无帧率损失）默认开启，菜单可关；构建时 three.js 独立成 vendor chunk（业务更新不重复下载渲染库）
- 打包体积：全量 ~4.9 MB（GLB 模型约 4.1 MB；JS/CSS/字体 gzip 后 ~0.5 MB）；GLB 量化压缩（KHR_mesh_quantization，GLTFLoader 原生支持）、字体自托管（无第三方请求）、无用的备份模型不进 dist（见 `scripts/optimize-models.mjs` 与 DEPLOY.md）

## 已知边界

- 闪光干扰的三个近似口径（维基只给定性描述/未公开的部分）：①致盲随距离/角度的衰减曲线无公开公式——按"≤18m 满时长、70m 线性归零 + FOV 内直视/背对 ×0.08"参数化（维基确认的行为锚点：直视满时长、背对轻微短暂、超远免疫）；②Phoenix Curveball 飞行速度无公开值——按对局观察 ~9m 落点 ÷ 0.6s 折算 15 m/s；③斯凯鹰的"敌方操控"为行为近似（瞄点实时跟玩家视线前方 2.2m，急转减速），原技能由施放者实时驾驶
- Bot 的还击由"击杀时限"判定 + 开火表现模拟（非真实弹道对射，不造成伤害），无经济/买枪系统
- 地面加速常数 18.75 m/s² 为社区实测单源口径（"with Rifle, scales with speed" 的具体曲线形状无公开数据），按"目标速度等比缩放"外推到静步/蹲/持刀各档位；减速摩擦 28.6 m/s² + 3.3/s 阻尼虽为社区推导，但与 Riot 开发者 Riot_Classick 公布的官方急停里程碑（61% 速度 0.055s / 25% 0.104s / 停稳 0.160s）逐点吻合（模型 0.049/0.104/0.147s）；跳跃重力/初速未随本轮改动（社区推导重力 21、跳高 1.2m 为单源且口径存疑——可能含蹲跳，不影响 Bot 对枪场景）
- 后坐力弹道的垂直爬升幅度为调校近似：多源核验（Riot 官方 Data Drop / Fandom 维基 / bo3.gg / 中文社区攻略）均无公开角度值，外挂补偿脚本的像素表不予采用——幅度锚定社区共识（明显小于 CS AK 的 ~11°，约其 1/3），形状（前 3 发缓、4-9 发陡、平台期）与水平方向（先右后左）经四个独立来源交叉一致
- 枪口"热浪扭曲"折射已实现（原型，热量门控）：仅开火后的热量窗（~1.8s）内走 RT+全屏扰动三段路径（程序化三正弦流动噪声，枪口屏幕区域径向椭圆掩码，强度∝连射热量），其余帧旁路回直渲零成本（交替窗口 A/B：冷却 12.1ms ≈ 直渲基线，注热 +2.8ms）；持枪层直渲保持锐利、两条路径色彩等价无跳变；菜单「画质 → 热浪扭曲」可整体关闭（设置持久化）
- 只支持平地（无楼梯/台阶）
